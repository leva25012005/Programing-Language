#include <iostream>

int main() {
  // Đây là một ghi chú đơn dòng (single-line comment).

  // Chúng ta có thể viết nhiều ghi chú đơn dòng liên tiếp nhau.
  // Đây là dòng ghi chú đơn thứ hai.

  /* Đây là ghi chú nhiều dòng (multi-line comment).
     Trình biên dịch sẽ bỏ qua toàn bộ phần nội dung này.
     Cách viết này còn được gọi là C-style comment. */

  std::cout << "Hello World.";

  return 0;
}